# 关于Web开发

我的理解：

- **Web前端**负责的主要是用户看到的界面(*interface*)，以及用户交互行为(*interaction*)。
- 后端的任务一是**接收前端发送的请求**，二是**处理用户请求，并操作数据库**，三是**将结果返回给前端**。
- **客户端**指的是在用户电脑上运行的软件，比如浏览器。**服务端**指的是在远程服务器上运行的软件，比如*Web服务器*。
- 区别：**前/后端**主要指的是职责的区分，比如"我是负责前端的"或"这段代码是后端的"。而**客户/服务端**主要指的是代码的运行环境。

打个比方，如果一个网页被看作一家饭店：

- 用户就是顾客；
- **前端**就是服务员，让顾客看到饭店的店面(*interface*)，以及接受用户的点菜(*request*)。
- **API**就是服务员和厨师之间的对讲机，将顾客点的菜告知给厨师。
- **后端**就是厨师，用于处理顾客的点菜，并把做好的菜给服务员。
- **客户端**就是用餐区，**服务端**就是饭店的厨房。

以bilibili为例，**客户端**就是加载B站网页的浏览器或软件，**服务端**就是B站的服务器，**前端**就是B站的网页，将B站主页展示给user。当user试图点开一个视频时，前端收到该*request*，通过**API**将该信息传输给后端，后端便从服务器中获取该视频的信息并返回给前端，前端将这些信息展示给user。

# 关于后端的工作

我的理解：

后端要做的，大体上就是**处理前端发送来的请求，操作数据库，最后返回给前端**。可以说，后端的工作要考虑到网页运行的整套逻辑。

仍以B站举例，当用户试图点开某个视频，后端接受到这个请求时，要考虑的有：

- 判断用户是否登录
- 获取用户的默认播放设置
- 获取用户权限
- 获取视频信息(包括是否已下架，是否限制大会员观看等)
- 获取视频的弹幕信息
- 获取视频的评论信息
- ......
- 更新视频的播放量、用户的观看历史等信息
- 返回给前端

由此可见，虽然后端操作的是不可见的部分，但是需要考虑的事情十分多。只有将一个操作相关的**所有信息**都考虑进去，才能降低出bug的可能，实现业务逻辑。

# 编程语言和后端

编程语言，字面意思，是一门"语言"，计算机使用的语言。当我们用编程语言编写出一个程序时，实际上就是将我们人类的诉求"翻译"成计算机能看懂的程序。因此，在后端工作中，编程语言的作用就是**将业务逻辑转换成计算机能看懂的代码**，用语言编写出程序实现需要达到的目的。

例如，在上述例子中，为了**判断用户是否登录**，我们需要用编程语言编写一个程序，这个程序会判断用户是否登录，并返回结果。

# 关于架构

根据我的学习与理解：

目前我们使用的架构是**前后端分离**。它与前后端不分离的架构区别主要是：

- **前后端不分离**：服务端进行所有的请求处理、数据库操作、生成HTML页面。每次用户发送请求，都由服务端直接接收并处理，并且生成和渲染HTML页面并返回给用户。
- **前后端分离**：前端负责页面渲染，后端负责数据处理。当前端发送请求时，请求会发送给后端，后端根据请求处理数据，并通过API将数据以JSON格式返回给前端。前端收到数据后，会在客户端中进行数据处理，并渲染页面。
- 最关键的区别：**渲染页面的工作，前者由服务端完成，后者由客户端完成。**

因此，前后端分离的优点：

1. **提升渲染页面速度**：前后端分离，服务端负责数据处理，渲染页面的工作由客户端完成，因此，用户在访问页面时，页面的渲染速度会更快，提升用户体验。
2. **便于开发时分工进行**：负责前端和后端的人员只需要分别开发，不需要同时开发，且如果需要进行调试或修改等操作，能很大程度上简化工作。
3. **降低开发者的学习量和门槛**：入门前/后端，只需要学习对应方向的语言和相关知识，降低了入门门槛。
4. **降低服务端压力**：服务端无需进行HTML页面的渲染，降低了服务端的压力，更有利于维护。

同时，我认为它可能带来的实际问题：它可能导致整体开发变得更加复杂，倘若前后端没有协调好，出现API不对应或变量名不照应等细节问题，容易导致出现bug。

# 个人规划

## 我为什么想要入坑后端方向

1. 出于**现实因素**：后端方向的岗位似乎相对较多，在就业时我希望具备更强的竞争力以及更丰富的选择。
2. 出于**自身兴趣**：相较于ML的前沿、前端的交互性、core的深入探究等，我对后端更感兴趣：在我眼里，如果我有幸能参与Web后端开发，我会对成为网页背后处理着一个个请求、一次次操作数据库的开发者十分激动，并且会十分投入的参与开发。
3. **技能培养**：我认为参与学习入门后端，会让我积累更丰富的技术和能力。

## 我希望通过学习获得的：

1. 坚实的**code能力基础**，无论将来从事什么相关工作，这都十分重要。
2. 丰富的**知识和技能**，关于计算机的知识和技能当然是越丰富越好，虽然现在有AI，但是我相信丰富的知识结合我的思考和动手能力，可以开辟出一片属于我的小天地。
3. **学习能力本身**，我们软件从业者需要活到老学到老，通过有效地学习，我能够保持并培养我的学习能力，面对可能出现的任何新事物都能绰绰有余。
4. 更多的**伙伴和榜样**，在学习过程中我希望结识更多伙伴，在以后的人生中拥有讨论和学习的空间。
5. 属于我自己的**开创性成果**。如果我的学习到达一定程度，我希望尝试创造一些原创性的成果，既满足自己的兴趣，也能在自己的人生绘卷上着下浓墨重彩的一笔。
